// ExtPay's payment-success relay: the module's top-level code registers a
// window message listener when run as a content script on extensionpay.com.
// Side-effect import is all that's needed here.
import 'extpay'
